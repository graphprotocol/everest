query projectSearch($text: String) {
    projectSearch(text: $text) {
      id
      ipfsHash
      name
      avatar
      description
      categories {
        id
        name
        parentCategory {
          id
          name
        }
        subcategories {
          id
        }
      }
    }
  }