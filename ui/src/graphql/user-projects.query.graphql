query everestUserProjects($id: ID!) {
    user(id: $id) {
      id
      projects {
        id
        name
        avatar
      }
      delegatorProjects {
        id
        name
        avatar
      }
    }
  }